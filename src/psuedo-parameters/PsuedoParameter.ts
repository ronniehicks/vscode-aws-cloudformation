import { AccountId, NotificationARNs, NoValue, Partition, URLSuffix, Region, StackId, StackName } from '.';

export type PsuedoParameter = AccountId | NotificationARNs | NoValue | Partition | Region | StackId | StackName | URLSuffix;
