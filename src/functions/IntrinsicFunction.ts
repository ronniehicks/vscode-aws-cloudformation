import { And, Base64, Equals, FindInMap, GetAtt, GetAZs, ImportValue, Join, Not, Or, Ref, Select, Split, Sub, If } from ".";

export type IntrinsicFunction = And | Base64 | Equals | FindInMap | GetAtt | GetAZs | If | ImportValue | Join | Not | Or | Ref | Select | Split | Sub;
